import { Typography } from "@mui/material";

const DashboardPage = () => (
  <Typography variant="h4" component="h1" gutterBottom>
    Municipal Dashboard (Placeholder)
  </Typography>
);

export default DashboardPage;
